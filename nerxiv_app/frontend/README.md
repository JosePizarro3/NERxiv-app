# Frontend README

This directory will contain the React frontend for NERxiv-app.

## Setup (TODO)

The React frontend will be set up to run under `/ui` route.

For now, this is a placeholder. To develop the React frontend:

1. Initialize a React app in this directory
2. Configure it to work with the FastAPI backend
3. Set up routing to serve under `/ui`

## Development

```bash
# Navigate to frontend directory
cd nerxiv_app/frontend

# Install dependencies (when package.json is created)
npm install

# Run development server
npm run dev
```
